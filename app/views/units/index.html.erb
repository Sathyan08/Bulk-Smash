<div class="top-content">
  <h1>List of All Measurement Units</h1>
</div>

<%= render "foods/food_bar" %>

<div class="content">
  <h4>List of All Measurement Units:</h4>
  <ul>
    <% @units.each do |unit| %>
      <li><%= link_to unit.name, unit_path(unit) %></li>
    <% end %>
  </ul>
</div>
